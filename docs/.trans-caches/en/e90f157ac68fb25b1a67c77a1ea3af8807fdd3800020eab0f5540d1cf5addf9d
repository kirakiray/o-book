In the above example, we set up a homepage (`index.md`), which means the homepage will only include the top navigation component and will not display the left document navigation and right article navigation of document-type pages.